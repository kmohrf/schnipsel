#!/bin/sh

set -eu

APP_USER=schnipsel
APP_HOME=/var/lib/$APP_USER
APP_ETC=/etc/$APP_USER
APP_BACKUPS=/var/backups/$APP_USER

if [ "$1" = "remove" ]; then
    if getent passwd "$APP_USER" >/dev/null; then
        deluser --quiet "$APP_USER"
    fi
elif [ "$1" = "purge" ]; then
    rm -rf "$APP_ETC"
    rm -rf "$APP_HOME"
    rm -rf "$APP_BACKUPS"
    rm -f /etc/uwsgi/apps-*/$APP_USER.ini
fi

set +eu

# dh_installdeb will replace this with shell code automatically
# generated by other debhelper scripts.

#DEBHELPER#

exit 0
